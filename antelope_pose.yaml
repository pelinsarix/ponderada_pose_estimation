
# Caminhos
path: ./
train: ./train_data
val: ./train_data  # Na prática usar conjunto diferente

# Keypoints
kpt_shape: [8, 3]  # 8 keypoints com (x, y, visibility)

# Classes
names:
  0: antelope

# Parâmetros obrigatórios
nc: 1  # número de classes
flip_idx: [1, 0, 3, 2, 5, 4, 7, 6]  # conexões para flip horizontal
    